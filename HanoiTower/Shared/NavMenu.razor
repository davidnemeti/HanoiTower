<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">Hanoi Tower</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="hanoi-explanation" Match="NavLinkMatch.All">
                <span class="oi oi-layers" aria-hidden="true"></span> Hanoi Explanation
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="hanoi-solution">
                <span class="oi oi-list" aria-hidden="true"></span> Hanoi Solution
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
